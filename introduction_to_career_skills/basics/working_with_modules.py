import random

numbers = [1,2,3,4,5]

random.shuffle(numbers)
print(numbers)

# Here is a simple example of a module we can import a module and work with its functions by calling it
# we imported random and used one of its functions like shufle modules help to use already made and verified code, this helps save time
